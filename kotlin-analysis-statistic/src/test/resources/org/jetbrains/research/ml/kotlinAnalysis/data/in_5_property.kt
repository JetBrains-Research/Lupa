public fun a() {
    val rangeDotsInt = 1..10

    val rangeUntilInt = 7 until 16

    val rangeUntilLong = 3000000000 until 4000000000

    val rangeDotsChar = "a".."c"

    val rangeDownTo = 13 downTo 1

    val booleanExpression = (5 in 2 until 100) && (8 in 1..10) && (1 in -1..1)

    var rangeToExpression = "b".rangeTo("d")
}
