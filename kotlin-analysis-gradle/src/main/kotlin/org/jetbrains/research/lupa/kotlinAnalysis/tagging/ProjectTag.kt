package org.jetbrains.research.lupa.kotlinAnalysis.tagging

/** Tag for project which describes it's content of theme. */
enum class ProjectTag(val value: String) {
    ANDROID("android"),
    OTHER("other");
}
